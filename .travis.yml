sudo: required
dist: trusty

# install the stable release chefdk.
addons:
  apt:
    sources:
      - chef-stable-precise
      - sourceline: 'deb http://download.virtualbox.org/virtualbox/debian contrib'
        key_url: 'http://download.virtualbox.org/virtualbox/debian/oracle_vbox.asc'
    packages:
      - virtualbox-4.3
      - vagrant
      - chefdk

install: echo "skip bundle install"

# Ensure we make ChefDK's Ruby the default
before_script:
  - eval "$(/opt/chefdk/bin/chef shell-init bash)"

script:
  - /opt/chefdk/embedded/bin/chef --version
  - /opt/chefdk/embedded/bin/rubocop --version
  - /opt/chefdk/embedded/bin/rubocop
  - /opt/chefdk/embedded/bin/bundle install
  - /opt/chefdk/embedded/bin/bundle exec rspec spec
  - /opt/chefdk/embedded/bin/chef exec kitchen test
